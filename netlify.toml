[build]
base = "mental-health"
command = "npm install && npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[env]
  [env.production]
    [env.production.vars]
    VITE_API_BASE = "https://your-production-api.com/api"

[functions]
directory = "functions"
